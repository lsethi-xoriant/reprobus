<% provide(:title, 'Payment Details') %>
<div class="section">
  <div class="row">
    <div class="col s12 m12 l12">
        <%= render partial: 'itinerary_details' %>

      	<%= render partial: 'invoice', collection: @invoices %>
    </div>
  </div>
  
  <div class="row">
    <div class="input-field col s6" >
      <a href="<%= edit_itinerary_price_path(@itinerary_price) %>"  class="waves-effect waves-light btn left deep-purple darken-1" >Back to Itinerary Price</a>
    </div>
  </div>
</div>  