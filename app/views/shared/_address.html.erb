

<%= render partial: 'shared/standard_grouped_textfield', locals: { field: "street1", f: f, name: "Street 1" } %>
<%= render partial: 'shared/standard_grouped_textfield', locals: { field: "street2", f: f, name: "Street 2" } %>
<%= render partial: 'shared/standard_grouped_textfield', locals: { field: "city", f: f} %>
<%= render partial: 'shared/standard_grouped_textfield', locals: { field: "state", f: f} %>
<%= render partial: 'shared/standard_grouped_textfield', locals: { field: "zipcode", f: f, name: "Post Code" } %>
<%= render partial: 'shared/standard_grouped_textfield', locals: { field: "country", f: f} %>
