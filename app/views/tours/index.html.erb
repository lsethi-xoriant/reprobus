<% provide(:title, 'All Tours') %>
<h1>All Tours</h1>

<p><strong>Create new <%= link_to "Tour", new_tour_path %></strong></p>

<%= will_paginate %>

<ul class="users">
	<%= render @tours %>
</ul>

<%= will_paginate %>