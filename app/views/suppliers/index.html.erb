<% provide(:title, 'Suppliers') %>

<div class="section">
  <a href=<%= new_supplier_path %> ><button type="button" class="btn waves-effect waves-light">Create New Supplier</button></a>
</div>

<div class="divider"></div>

<div class="section">
  <div id="table-datatables">
   <div class="row">
      <div class="col s12 m12 l12">
        <table id="ajax-data-table-simple" class="responsive-table display", data-source="<%= suppliers_path(format: :json) %>" >
          <thead>
            <tr>
              <th>Name</th>
              <th>Contact first name</th>
              <th>Contact last name</th>
              <th>Email</th>
              <th>Phone</th>
              <th>Edit</th>
            </tr>
          </thead>
          <tbody>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>