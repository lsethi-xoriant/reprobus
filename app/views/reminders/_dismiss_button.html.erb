<%= link_to 'Dismiss', 
      dismiss_reminders_path(
        dismiss_until: { 
          type: object.class.name,
          id:   object.id,
          date: Date.tomorrow 
        }
      ), 
      method: :post,
      data: { tooltip: 'Dismiss'},
      class: "input-field waves-effect waves-light btn deep-purple darken-1 tooltipped"
%>